//export default, significa que por defecto exporta la funcion
export const despedir = (nombre,  apellido) => {
    return`Adios ${nombre} ${apellido}`
}

export const  suma=(n1,n2)=>{
    return n1+n2;
}
//ó export{despedir, suma}